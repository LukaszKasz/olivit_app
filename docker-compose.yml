version: '3.8'

services:
  # PostgreSQL Database
  db:
    image: postgres:15-alpine
    container_name: app-start-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: app_start_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-start-network

  # FastAPI Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: app-start-backend
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/app_start_db
      SECRET_KEY: your-secret-key-change-in-production-please
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - app-start-network
    restart: unless-stopped

  # React Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: app-start-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - app-start-network
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local

networks:
  app-start-network:
    driver: bridge
